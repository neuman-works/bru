const runner = () => {
    /* execute getSpreadsheet
        - check if auth required
        - send user to auth link
        - get auth code
        - pass to access token
        - pass access token to getSpreadsheet
    */
}


module.exports = {
    runner
}